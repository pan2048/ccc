EXTERNAL_FILES := $(filter-out $(wildcard external/*test.c), $(wildcard external/*))
EXTERNAL_FILES := $(filter-out $(wildcard external/*.h), $(EXTERNAL_FILES))
EXTERNAL_FILES := $(filter-out $(wildcard external/*.o), $(EXTERNAL_FILES))
EXTERNAL_FILES := $(filter-out external/Makefile, $(EXTERNAL_FILES))